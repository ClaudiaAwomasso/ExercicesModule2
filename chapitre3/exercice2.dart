void main() {
  //Exercice 3 : Écrire une fonction qui calcule la moyenne
//coefficiée des notes d’un élève de la Terminale.
  int resultat = moyenneCoefficie(14, 3);
  print(resultat);
}

int moyenneCoefficie(int note, int coefficient) {
  return note * coefficient;
}
